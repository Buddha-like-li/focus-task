# Focus Task Windows 客户端协作规则

本文件是本仓库所有代码、文档、测试和发布改动的必读入口。开始工作前，先阅读本文件、[功能总账](docs/feature-ledger.md)，并按改动范围阅读 [客户端审计](docs/code-audit-2026-07-21.md)、[发布规范](docs/releasing.md) 和相关任务记录。

## 项目边界

- 本仓库只保存 Vue/Tauri Windows 11 客户端、Windows NSIS 发布自动化和客户端治理文档。
- 服务端源码、容器编排、服务镜像、数据卷归档、数据库和服务端凭证属于独立的本地服务仓库，禁止加入本仓库或推送到客户端 GitHub 仓库。
- 客户端固定调用 `http://127.0.0.1:18765`。任何 API 契约变动必须由客户端和服务端各自建立任务记录并共同验收。
- 客户端不读取、备份、迁移或删除旧安装目录中的任务数据。旧数据兼容/迁移是独立 P0 任务，未有书面验收前不得宣称已解决。
- Web、macOS、Linux、Android、iOS 和移动端不是当前交付范围；不得恢复其打包、部署或运行时代码。

## 当前仓库状态

- 本仓库是从 `D:\工作工具\focus-task-client` 的已审核 Windows 客户端快照重建的独立候选仓库；旧仓库提交历史、`legacy-origin` 和服务端历史均不在本仓库可达历史中。
- 当前候选分支是 `codex/client-clean-history`。在独立审核通过且集成人书面批准前，不得合入、打 tag、创建 Release 或推送远程。
- `origin` 只允许指向 `git@github.com:Buddha-like-li/focus-task.git`；不得配置或使用 `legacy-origin`。

## 必须遵守的改动流程

1. 一个可独立验收的功能或缺陷只使用一个独立分支（或独立工作树）和一名实现者。不要把无关改动混入同一分支。
2. 实现者完成后，必须由未参与实现的审核者进行独立代码审查，重点检查行为回归、客户端/服务 API 契约、数据兼容、测试、凭证边界和文档。
3. 审核发现的问题必须由指定修复者在该功能分支修复；审核者或另一名独立审核者必须复核修复结果。
4. 只有集成人可以执行最终测试、更新功能记录和发布说明、合入集成基线以及推送远程。实现者和审核者不得绕过该关卡直接合入或推送未审核功能。
5. 每项合入都必须更新 `docs/feature-ledger.md`：记录版本或阶段、用户可见行为、提交或 tag、测试/文件证据，以及仍未解决的限制。
6. 修改发布、认证、桌面生命周期或 API 契约前，必须先检查 [客户端审计](docs/code-audit-2026-07-21.md) 中的未解决风险，并为修复建立独立分支。

## 文档职责

- `docs/feature-ledger.md`：当前已实现客户端能力、版本证据和已知风险的总账。
- `docs/code-audit-2026-07-21.md`：客户端边界和发布风险的待修复结论，不代表修复已完成。
- `docs/releasing.md`：Windows 版本号、tag、CI 与 Release 操作规范。
- `docs/task-records/`：每项任务的实现、审核、复核和集成证据的唯一承载位置。

## 可追溯性与分支

- 每条任务记录必须包含用户目标、基线 SHA、功能分支、实现 SHA、审核 SHA、实现者、审核者、集成人、验证命令和结果。修复后必须记录复核结论。
- 功能分支必须从集成人确认的当前客户端集成 SHA 创建；不得从遗留仓库、历史 tag 或服务仓库创建分支。
- 集成人由当前任务负责人显式指定；未指定时不得开始合入或推送。

## 风险门禁

- P0 必须修复并经独立复核后才能合入、打 tag 或发布；例外必须在任务记录中取得用户或集成人的书面批准。
- P1 必须有负责人、独立分支和明确的延期记录，才可继续发布。
- P2 可延后，但必须在功能总账或审计报告中保持可追溯的待办状态。
- 任务记录文件名使用 `docs/task-records/<YYYY-MM-DD>-<branch-slug>.md`；格式见 [任务记录说明](docs/task-records/README.md)。
