# 任务文档自动保存并定位

## 用户目标与范围

- 用户反馈本地创建的任务点击文件夹图标后提示“请先下载后重试”。用户期望本地和远程任务均可直接打开任务文档所在文件夹。
- 本任务将文件夹操作改为先把当前任务 Markdown 写入受控 Windows 文档目录，再在资源管理器中定位该文件。
- 不读取旧安装目录、不访问容器内导出路径、不改服务 API、不改任务数据或下载资产。

## 可追溯性

| 字段 | 记录 |
|---|---|
| 日期 | 2026-07-29 |
| 基线 SHA | `9cabc1025331e3f1c7681165354926b636368c04` |
| 功能分支 | `codex/report-open-local-file` |
| 实现者 | `/root` |
| 集成人 | `/root` |
| 实现 SHA | `9db28a2ab21766663bef963e5dbec787f267199f`（首次实现） |
| P1 修复 SHA | `68381374fdf5885e04aac23f5fe919963f3aa511` |
| 审核者 | 初轮独立审核者待回填；修复复核待重新指派 |
| 审核 SHA | 初轮结论已确认 P1；修复复核 SHA 待回填 |

## 风险与门禁

- 旧桌面数据兼容 P0 不受影响：本功能只写入用户主动请求的 `Documents\\Focus Task\\Reports` 文件，不读取、迁移、覆盖或删除旧任务数据。
- 客户端/服务 API 契约 P1 不受影响：继续使用现有 Markdown 快照字段，不新增或修改接口。
- 文件名继续由客户端生成并由 Tauri 端二次校验，服务端返回的容器路径不会传给本机文件操作。

## 实施、审核与验证

### 实施

- 文件夹按钮改为“保存并打开任务文档所在文件夹”。桌面端调用既有受控 Tauri 命令，将当前 Markdown 写入 `Documents\\Focus Task\\Reports` 后再由资源管理器定位；浏览器端继续使用普通下载回退。
- 不再要求用户先点击下载，也不再把服务端导出路径或容器路径传给本机文件操作。
- 更新视图测试，覆盖下载与“保存并打开”均只使用任务快照内容、不泄露服务端导出路径。

### 独立审核

- 首轮审核发现 P1：Rust 端此前通过 `fs::write` 保存同名文件，会覆盖用户在 `Documents\\Focus Task\\Reports` 中手工编辑过的 Markdown；前端按钮禁用不能覆盖跨调用竞态。

### 修复复核

- 修复 `68381374fdf5885e04aac23f5fe919963f3aa511`：Rust 端改用 `OpenOptions::create_new(true)` 原子创建。若同名普通文件已存在，命令不写入，直接返回既有路径供资源管理器定位；非普通文件会返回可读错误。
- 新增 Rust 覆盖：首次创建、同名用户文件不覆盖、重复调用保留首次内容；新增视图覆盖：首个“保存并打开”请求未完成时，连续点击只会发起一次调用。
- 独立复核结论待审核者回填。

### 验证

- `cargo test`：7 项通过，覆盖首次创建、已有普通文件不覆盖、重复调用保留首次内容。
- `cargo fmt --check`、`git diff --check`：通过。
- `npm test -- --run`：12 个测试文件、37 项通过；其中报告文件动作与报告视图定向测试共 10 项通过。
- `npm run build`：通过。
