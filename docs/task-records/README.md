# 任务记录

每个可独立验收的功能或缺陷都必须在本目录创建一份任务记录，文件名使用
`<YYYY-MM-DD>-<branch-slug>.md`。本目录是实现、审核和集成证据的唯一承载位置；
`docs/feature-ledger.md` 只保留已合入功能和未解决风险的摘要。
分支名中的 `/` 必须替换为 `-`，不得让文件名隐式创建子目录。

每份记录必须包含：

- 用户目标与范围
- 基线 SHA、功能分支、实现者和集成人
- 实现 SHA、审核者、审核 SHA、审核结论和修复复核结论
- 验证命令、结果和仍未解决的风险
- P0/P1 延期或发布例外的书面批准（如适用）
