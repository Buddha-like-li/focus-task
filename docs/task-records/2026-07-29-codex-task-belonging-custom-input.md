# 任务归属可新增

## 目标与范围

- 用户目标：任务详情中的“任务归属”不再受固定下拉值限制；用户可输入新的归属，也能从已有建议中选择。
- 本任务仅修改 Windows 客户端任务详情界面及其组件测试。
- 不修改本地服务 API、Docker、凭证、用户数据、远程仓库或发布配置。

## 可追溯性

| 字段 | 记录 |
|---|---|
| 日期 | 2026-07-29 |
| 基线 SHA | `9cabc10` |
| 功能分支 | `codex/task-belonging-custom-input` |
| 实现者 | `task_belonging_impl` |
| 审核者 | 待集成人指定 |
| 集成人 | `/root` |
| 实现 SHA | 初始实现 `6219243c81ffa1fcb378be0dfe7d9dcf3e2fc04b`；首次审核修复 `55074abe6c024a9a5d4ed41e1564750128af140c`；并发与服务端空白值修复 `c06370e091b87d5cea17346e9ddc61c84ac13f7b` |
| 审核 SHA | 待独立审核后填写 |

## 设计与边界

- 详情面板使用文本输入框和建议列表：用户可直接输入至多 100 个字符的归属，也可选择建议值。
- 建议列表保留既有预设值，并从当前未删除任务中收集已使用的归属；正在编辑任务的值也会保留在建议中。
- 保存仍复用 `taskStore.updateTask`，不新增或变更客户端与服务端 API 契约。
- 同一任务归属字段保存期间会禁用输入；极短时间内收到的同一任务后续变更只保留最新值，并在前一请求结束后串行保存，避免乐观更新失败的回滚覆盖后一次输入。
- 客户端接收服务端任务时会去除归属字段首尾空白；空白或缺失值统一为“项目管理”。建议列表也只保留去除首尾空白后的非空值。

## 风险与门禁

- 已检查 `docs/code-audit-2026-07-21.md`。本任务不涉及发布、认证、桌面生命周期、迁移或服务 API 契约。
- 旧桌面数据兼容 P0 不受本界面改动影响：本任务不读取、迁移、备份或删除任何用户数据。

## 实施、审核与验证

### 实施

- 初始实现 `6219243c81ffa1fcb378be0dfe7d9dcf3e2fc04b` 将固定下拉改为可输入的建议列表，并补充初始组件测试。
- 独立审核指出空白归属会与客户端默认值不一致，且缺少可访问性关联和失败回显覆盖；修复提交 `55074abe6c024a9a5d4ed41e1564750128af140c` 将输入值先去除首尾空白，空值显式保存为“项目管理”，保存失败时恢复已保存值，并添加标签关联与回归测试。
- 复核再发现同一字段的并发乐观更新可能产生乱序回滚，且服务端历史空白归属会显示或进入建议；修复提交 `c06370e091b87d5cea17346e9ddc61c84ac13f7b` 引入单字段 busy/串行队列、输入禁用、失败后最新值保护，并在任务标准化和建议收集时清理空白值。

### 独立审核

- 待集成人指定未参与实现的审核者。

### 修复复核

- 如审核发现问题，由集成人指定修复者并记录复核结论。

### 验证

- `npm test -- --run src/components/DetailPanel.test.ts src/stores/taskStore.test.ts`：通过，7/7。覆盖自定义归属输入、预设/既有自定义建议值、100 字符限制、清空/空白默认值、保存失败提示与输入回显、连续变更串行保存，以及服务端空白/带空白归属标准化。
- `npm test -- --run`：通过，43/43。
- `npm run build`：通过，包含 `vue-tsc --noEmit` 与生产构建。
- `git diff --check`：通过。

### 集成

- 仅集成人可在独立审核和复核完成后填写合入、推送或发布信息。
