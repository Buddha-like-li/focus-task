# v2.3.9 桌面端登录状态修复发布

## 用户目标与范围

- 用户要求修复桌面端无法保存登录状态，并能在登录页选择已记住账号；完成后发布 Windows 客户端用于本机验证。
- 本次只发布 Windows 客户端，不包含服务端源码、镜像、容器编排、数据卷、账号密码或数据库，也不改变本地服务 API。

## 可追溯性与责任

| 字段 | 记录 |
|---|---|
| 发布基线 | `bc6fd16` |
| 功能整合分支 | `codex/auth-session-selector-integration` |
| 原生实现 SHA | `84f42a2` |
| 前端实现 SHA | `d27b000`、`0b0c753` |
| 功能整合 SHA | `b7991db` |
| 原生审核者 | `/root/auth_dpapi_reviewer` |
| 前端审核者 | `/root/auth_frontend_reviewer` |
| 集成人与发布执行者 | `/root` |
| 目标版本与 tag | `2.3.9` / `v2.3.9` |

## 发布前验证与审核

- 原生审核：P0 0 项、P1 0 项；DPAPI 当前用户加密、原子替换、多账号上限、令牌定向清理、命令注册和跨进程锁均已复核。
- 前端审核：首轮发现两条账号下拉测试的异步等待缺失；修复 `0b0c753` 后复核通过，P0/P1 均无。
- 整合验证：`npm test -- --run` 为 21 个文件、83/83；`npm run build` 通过；`cargo test` 为 15/15；`cargo fmt --check` 和 `git diff --check` 通过。
- 本地 Windows NSIS 安装包已生成：`Focus Task_2.3.9_x64-setup.exe`，SHA-256 为 `FF86C10EBDC3D95FDE671680705931EFA6BA255ADBB43B5242146D689227F11E`。正式 `.sig` 和更新清单仍由 tag 触发的签名工作流生成。
- 发布说明为纯中文，版本号同步更新桌面配置、Rust 包和前端包。

## 风险、延期与发布批准

- **P0 受控测试例外：旧桌面任务数据兼容。** 本版本不读取、迁移、覆盖、备份或删除旧任务数据；既有例外仅允许用户安装并验证本机服务连接，不得宣称旧任务自动兼容。
- **P1 延期：客户端与本机服务端登录接口的端到端验收。** 本次未变更服务 API；用户已要求发布后在本机安装验证登录恢复和账号切换，异常将另建独立客户端或服务端任务。
- **P2：账号清理入口与遗留凭据。** 详见两个认证功能任务记录；不阻塞本次修复发布。
- 用户此前明确要求推送后无需等待持续集成完成；因此 tag 推送后可交付推送结果，但持续集成成功前不得宣称公开安装包已可用。
