# Windows 客户端垃圾桶界面

## 用户目标与范围

- 用户目标：普通删除后的任务进入垃圾桶；用户可恢复，或在明确的中文二次确认后彻底删除。
- 彻底删除的用户可见范围：服务端负责删除任务记录、附件、任务文档、快照和服务端报告副本；客户端在服务端成功后清理 `Documents\Focus Task\Reports` 内该任务的应用托管 Markdown 副本。
- 本任务只覆盖 Windows 客户端的 API 接入、垃圾桶状态、导航、确认交互和受控本机副本清理。不得修改服务端、Docker、服务凭证、发布配置或数据卷。

## 可追溯性

| 字段 | 记录 |
|---|---|
| 日期 | 2026-07-30 |
| 用户目标 | 删除任务后可恢复；用户确认后彻底清理关联内容 |
| 基线 SHA | `13006558bf997b0291590b8714f2114e7fe07235` |
| 功能分支 | `codex/trash-client-ui` |
| 实现者 | `trash_client_impl` |
| 审核者 | `/root/auth_frontend_reviewer`（2026-07-30 只读独立复核） |
| 集成人 | `/root` |
| 实现 SHA | 初始实现 `0631ece6c655edab5670997d9f043a8372bfceab`；P1 修复 `06e3db48c3355b2f9e13e77547301f77be1f9ca4` |
| 审核 SHA | `06e3db48c3355b2f9e13e77547301f77be1f9ca4`（复核对象；只读审核无独立代码提交） |
| 服务 API 契约 | `GET /api/tasks/trash`、`POST /api/tasks/{id}/restore`、`DELETE /api/tasks/{id}/permanent`；永久删除响应含 `cleanup_pending` |

## 实现设计

- 普通删除仍调用兼容的 `DELETE /api/tasks/{id}`，但客户端全部可见入口均称为“移入垃圾桶”。已完成任务的批量移动先经中文确认；部分失败时显示成功数和失败数，绝不静默。
- 新增独立 `trashStore`，仅使用垃圾桶接口，不复用工作台的已删除任务缓存。账户切换、退出和登录失效时清空垃圾桶状态并废弃旧请求响应。
- 垃圾桶页面不渲染任务详情、附件下载、文档预览或打开文件夹入口，避免已删除任务绕过服务状态。
- 恢复成功后重新拉取工作台任务；永久删除成功后立即从垃圾桶和工作台内存缓存中移除。
- 永久删除使用页面内受控中文对话框，明确不可恢复、子任务会一并处理，并在处理期间禁用取消、关闭和重复确认。服务端返回 401、403、404、409 时转换为可读中文提示；尤其 409 会说明任务可能与已有任务冲突或不满足本机安全限制。
- 永久删除响应的 `cleanup_pending=true` 表示服务记录已删除、服务端文件清理待处理。客户端保持任务从垃圾桶移除，但显示“服务记录已删除，文件清理待处理”，不得误报为全部清理完成。

## 本机报告副本清理边界

- Tauri 命令只接受正整数任务 ID，绝不接受目录、文件名或任意路径参数。
- 命令固定枚举 `Documents\Focus Task\Reports` 的直接子项；仅删除普通文件，拒绝符号链接且不递归进入目录。
- 仅严格匹配 `task-<任务ID>-<字母数字或连字符时间标记>.md` 的应用托管任务 Markdown 副本。相同目录中的其他任务、汇总报告、非 Markdown 文件和子目录均保留。
- 同一受控文件名被用户手工编辑时仍属于该任务副本，按本次用户明确授权删除；用户另存到其他位置的文件不在范围内。
- 本机副本清理只会在服务端永久删除成功后触发。清理失败不会伪造服务端回滚，客户端显示“服务数据已删除，但本机报告副本清理失败”。

## 风险与门禁

- 已检查 `docs/code-audit-2026-07-21.md`：旧桌面数据迁移 P0 不由本任务处理；客户端不读取、迁移、备份或删除旧安装目录的数据。
- 客户端/服务 API 契约曾是 P1 门禁。服务端已独立实现、审核并在本机服务容器中联调，确认父子批次移入/恢复/彻底删除，以及需求转化任务同步删除保持本地模式和需求占用；远程任务、MinIO 和历史跨归属父子限制仍按服务任务记录保留。
- 本任务不创建 Release、不修改版本号、不打 tag、不推送远程。正式发布仍需独立审核、集成人合入、完整 Windows 构建与发布门禁。

## 验证

### 实现者验证

- `npm test -- --run`：23 个测试文件、104 项测试全部通过。
- `npm run build`：通过；`vue-tsc --noEmit` 和生产构建均成功。仅保留基线已有的动态导入分块提示，无错误。
- `cargo test`：17 项 Rust 测试全部通过，含受控任务报告副本清理的文件名、非递归和其他文件保留验证。
- `cargo fmt --check`：通过。
- `git diff --check`：通过。

### P1 审查修复与服务联调（已完成）

- 修复提交：`06e3db48c3355b2f9e13e77547301f77be1f9ca4`（`fix(垃圾桶): 防止陈旧删除与不安全副本清理`）。
- 审查发现一：永久删除父任务时，客户端此前只清理父任务 ID 对应的本机报告副本。现改为遍历服务垃圾桶响应中的完整 `subtasks` 树，对父、子、孙任务分别调用受控副本清理；清理失败仍不伪造服务端回滚，并显示涉及任务数量。
- 审查发现二：Tauri 清理命令此前只跳过文件符号链接，没有验证 `Focus Task` 和 `Reports` 目录链。现先规范化可信 Windows 文档根目录，使用不跟随链接的元数据检查拒绝符号链接、目录联接/重解析点和非目录组件，并确认最终目录仍位于文档根目录内；只在该实体目录中枚举直接普通文件。回归测试覆盖两级目录分别链接到外部临时目录，外部匹配 Markdown 保持不变。
- 审查发现三：同一账号中较早的任务列表请求可在“移入垃圾桶”成功后写回已删除任务。现为工作台列表增加顺序版本号，移动、彻底删除、服务端单项写入和外部替换都会使旧列表响应失效。
- 兼容性补充：旧服务镜像会接受兼容删除接口却没有垃圾桶恢复接口。现首次“移入垃圾桶”前同步验证垃圾桶列表接口；404/422 显示“本机服务版本不支持垃圾桶，请先更新服务镜像。”且绝不发送删除请求；成功结果仅缓存到当前登录会话，切换账号或退出后失效；网络和 401 原始错误保持不变。
- 修复验证：`npm test -- --run` 通过（23 个测试文件、111 项）；`npm run build` 通过；`cargo test` 通过（19 项 Rust 测试）；`cargo fmt --check` 与 `git diff --check` 通过。Windows 当前会话无创建目录符号链接特权（错误 1314），测试优先创建符号链接，受限时创建目录联接继续验证同一重解析点拒绝逻辑，未跳过该安全断言。
- 修复复核：`/root/auth_frontend_reviewer` 已于 2026-07-30 对 `06e3db4` 完成只读独立复核，三项 P1（父子副本清理、目录链接防护、陈旧列表响应）均通过。旧服务版本拦截作为同次兼容性补充已纳入复核范围。客户端/服务端 API 与容器联调已于同日完成；尚未创建 Release 或 tag。

### 独立审核

- 审核者 `/root/auth_frontend_reviewer` 已独立复核 `06e3db4`：父子副本清理、同会话陈旧列表防复活、目录符号链接/目录联接防护均无 P0/P1 问题。
- 服务 API 契约、垃圾桶树返回、彻底删除语义和本地服务容器联调已由独立服务任务完成：服务主线 `34109ea` 在全量 123 项服务测试通过后构建隔离镜像，真实 HTTP 验证父子批次与需求转化任务同步删除路径；当前本地服务容器健康运行于同一镜像摘要。该联调未读取、迁移或删除用户数据卷。

### 延期 P2

- Windows 文档根目录不存在时，受控本机副本清理会在规范化目录根时返回错误。当前行为不会创建目录、更不会清理任何外部路径；后续需独立决定“目录不存在”是否应无害地视为零个副本，并补充回归测试。
- 服务端彻底删除已成功但本机副本仍在异步清理时，垃圾桶状态的处理中标记已经结束，确认对话框短暂允许再次点击。后续需以独立交互修复把服务端请求和本机清理合并为同一禁用窗口，并验证重复点击不会产生第二次请求。

### 集成

- 集成人 `/root` 已将客户端分支合入本地主线 `e612818f9465f5df10b36ba3af98e856594513d8`。
- 服务 API 已完成独立复核、服务主线合入、隔离 Docker 容器联调和当前本地容器替换。服务同步删除保护合入 `34109ea653cf779ff1f2412dfb46588088f22312`；本机服务健康检查通过。客户端主线尚未创建新版本 tag 或公开 Release。
